<!DOCTYPE html>
<html>
<head>
  <title>NewspaperApp</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
	<div class="container">

		<header class="navbar navbar-inverse">
			<div class="row navbar-inner">
				<%= link_to "NEWSPAPER APP", root_url, class: "brand" %>
				<% if logged_in? %>
					<%= link_to "Log Out", logout_path, class: "btn pull-right" %>
					<strong class="navbar-text pull-right"><%= current_user.name %></strong>
				<% else %>
					<%= link_to "Log In", login_path, class: "btn pull-right" %>
				<% end %>
			</div>
		</header>

		<%= yield %>


		<footer></footer>
	</div>
</body>
</html>
